{
  "name": "@cookunity-seo-agent/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@cookunity-seo-agent/shared": "workspace:*",
    "@prisma/adapter-pg": "^6.19.3",
    "@prisma/client": "^6.5.0",
    "pg": "^8.13.1",
    "prisma": "^6.5.0"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
